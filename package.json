{
  "name": "seeker",
  "private": true,
  "version": "0.4.1",
  "type": "module",
  "description": "探索者 · Seeker — 本地优先的个人 AI 工作空间，整合对话、写作、资料、可编辑作品与任务 Agent",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.mjs",
    "test:e2e": "playwright test",
    "tauri": "tauri",
    "build": "tauri build --bundles nsis",
    "build:all": "tauri build"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@tauri-apps/cli": "^2.11.2",
    "esbuild": "0.28.2",
    "html2canvas": "1.4.1",
    "typescript": "^5.7.2"
  }
}
