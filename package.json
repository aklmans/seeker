{
  "name": "seeker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Seeker(探索者)— 本地优先的个人 AI Agent 平台:壳 + 可开关小应用(首个应用:求职工作台)",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.mjs",
    "tauri": "tauri",
    "build": "tauri build --bundles nsis",
    "build:all": "tauri build"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "@tauri-apps/cli": "^2.11.2"
  }
}
