# 第二轮验收记录

范围按 [开发计划](ROUND-2-PLAN.md)，各增量证据见 [开发进展](ROUND-2-PROGRESS.md)，使用方式见
[我的作品](CREATIONS.md)。五个功能增量与追加弹窗调整已实现，本机工程验收完成。
未打包、推送或发布。本记录区分已验证结果与待验证事项。

## 已验证

| 范围 | 证据 |
| --- | --- |
| 工作空间、启动页与数据设置 | 浏览器 UI 创建/切换/归档恢复、偏好重启、备份导入往返；桌面实际创建与重启恢复 |
| 求职按需开启 | 默认关闭、旧数据保留、显式选择持久化；失败不假启用；原求职回归主动开启后通过 |
| 作品保存与历史 | 双端版本事务、保存失败保留、复制、移除撤销、历史恢复、完整备份往返 |
| 导图 | 三种布局、节点增删改/拖动/折叠、撤销、SVG/Markdown 下载；选中分支 AI 预览和字段限制 |
| 个人样式与其他作品 | 收藏与默认值、重启和备份保留；对比表/时间线结构编辑；AI 风格与 Widget 预览需显式应用 |
| 静态分享 | PNG 真下载尺寸、长正文与当前交互状态、附加文字、比例切换竞争、英文窄窗口 |
| 离线分享 | 下载的 HTML 经 HTTP 打开可交互，重新打开初始化；父页访问和外网请求拒绝；Web/Rust 外壳校验 |
| 桌面文件 | 实际 PNG/PDF/SVG 写入下载目录；PDF 单页且无脚本；剪贴板读回与 PNG 逐像素一致 |

## 最后一次桌面贯通路径

使用独立验收副本，数据标识为 `dev.zhapar.seeker.round2`，未替换用户常用安装。
模型使用本机确定性 HTTP 夹具与 Ollama 协议，不读取或转发云端密钥。

1. 通过系统文件选择器导入 145 字节、51 字符的中文阅读安排 TXT，解析成功。
2. 从文件阅读页创建导图，经真实 Rust 生成通道收到 6 个节点。请求只有 system/user 两条消息，
   包含这次选中的正文，没有工具列表。
3. 将根节点改为“周末轻松阅读”，另一节点改为“周六上午专注阅读”，其余节点保留。
4. 从个人样式菜单应用“纸上阅读”，保存为 v2；原始导出预览显示 2966×554。
5. 解锁后加载最新编译的桌面副本，重新打开 v2，确认六个节点、两个修改文字与手绘参数保留。
   选择 16:9 并添加标题、中文来源和署名，实际导出 1600×900 PNG 与单页 PDF；读回并渲染核对内容。
6. 桌面聊天经真实 `show_widget` 工具调用生成“阅读打卡”，自动保存。点击两次后导出的 606×462 PNG
   保留数字 2；同一弹窗另存的离线 HTML 从数字 0 初始化，在浏览器中点击变为 1，重新加载回到 0。
7. 再次退出并启动，首页对话历史与作品引用恢复；从历史打开 Widget v1，源码、手绘风格和编辑入口可用。
   求职仍关闭、当前工作空间保留。新版导出与个人样式弹窗均实际输入并截图检查，取消表单不新增收藏。

## 自动化与构建

P1/P2 修复后重新执行前端门禁、Rust fmt 与默认配置构建；Rust clippy 和测试沿用上一轮结果，
本次未修改 Rust 源码。CI 已配置双浏览器安装与执行，本次未推送，尚未触发远端 CI。

- `npm test`：160 项通过。
- `npm run typecheck`：通过。
- `npm run test:e2e -- --workers=4`：本地 HTTP、Chromium 57 项与 WebKit 57 项全部通过，共 114 项。
- `npx playwright test --project=webkit --list`：从仓库配置列出 13 个文件、57 项测试。
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`：通过。
- `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings`：通过。
- `cargo test --manifest-path src-tauri/Cargo.toml`：244 项单测、1 项 parser CLI 集成测试通过，4 项忽略。
- `cargo build --manifest-path src-tauri/Cargo.toml`：最终默认配置实际输出 `Compiling app`，构建通过。
- `git diff --check`：通过。

用户追加的弹窗边框与表单调整已实现，并经浏览器深浅色、长预览、保存样式及 390px 英文窗口验证。
旧版和新版桌面窗口分别截图，已确认表单留白、控件高度、边框与底部操作区的修正；曾因锁屏中断的步骤现已补齐。

## 验收反馈修复与复现

- **P1：内置默认样式丢失身份。** 新增回归先在旧代码上失败，保存值预期 `preset: soft`，实际为
  `minimal`。保存内置样式时现显式传入 `preset.id` 对应的 `preset` 字段，由统一样式规范化生成完整参数。
  同一条 E2E 在 Chromium/WebKit 通过，覆盖设置保存、刷新后新建、持久化作品、下拉框“柔和”、
  全部样式参数与再次刷新重开；已有个人样式与写入失败测试也通过。
  桌面独立副本实际选择“柔和”作为默认值，新建“默认柔和验收”并保存 v2，退出重启后确认名称、颜色、
  间距 24、圆角 20 与设置默认值保留，已截图核对。验收后恢复副本原来的“纸上阅读”默认值。
- **P2：WebKit 证据依赖临时配置。** 先前分享 5 项和各增量的 WebKit 记录来自仓库外临时配置，
  不能通过当时的仓库配置直接复现。本次将 `webkit` project 纳入 `playwright.config.mjs`，并让 CI
  安装两个浏览器；`npm run test:e2e` 现在默认运行两者的全量测试。

从干净检出安装并复现（配置自动启动本地 HTTP 服务）：

```bash
npm ci
npx playwright install --with-deps chromium webkit
npm run test:e2e
```

仅检查或运行 WebKit：

```bash
npx playwright test --project=webkit --list
npm run test:e2e -- --project=webkit
```

此前已经保存过错误身份的默认值，需要在设置中重新选择并保存一次；本次修复不会自动重写已有作品。

## 仍需用户试用的部分

真实模型生成质量、复杂任意 Widget 的视觉兼容性、Windows 真机，以及普通用户能否顺利完成作品制作，
尚未经过试用验证。AI 链路的确定性测试只能证明调用、校验与保存行为。

PDF 当前是栅格图片页；固定比例会缩小完整长内容。离线 HTML 从源码初始化，不保存上次计算状态，
不支持依赖应用动作或网络的功能。公网托管链接、多人协作、XMind 文件和参考图样式提取属于后续范围。
