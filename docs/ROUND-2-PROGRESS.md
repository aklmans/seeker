# 第二轮开发进展

完整范围见 [ROUND-2-PLAN.md](ROUND-2-PLAN.md)。五个增量的功能均已实现，最后的桌面贯通验收正在收尾。

| 增量 | 状态 |
| --- | --- |
| 工作空间、设置、求职按需启用 | 已实现并完成本机验证 |
| 作品持久化、Widget 恢复与导出 | 已实现；本机验证见下文 |
| 可编辑思维导图、分支 AI 操作 | 已实现；本机验证见下文 |
| 个人样式与知识卡片/对比表/时间线 | 已实现；本机验证见下文 |
| 分享完善与整轮验收 | 功能与自动化检查通过；最终桌面贯通验收收尾中 |

## 第一增量证据

- 前端单测 149 项通过，typecheck 通过；经本地 HTTP 的 Playwright 全量 37 项通过。
- Rust 239 项单元测试通过、4 项忽略；1 项编译程序 parser 集成测试通过。fmt、Clippy 通过。
- 新回归先复现旧标记/数据自动开启求职及写失败假启用，再验证默认关闭、显式启用持久化和关闭后数据保留。
- 工作空间 UI 验证创建、改名、指令、切换、当前空间对话筛选、归档恢复、启动页、默认名称和首页展示偏好重启保留。
- 外观验证系统颜色变化、字号与密度真实生效。个人信息默认空白，写失败不更新真实记录。
- 历史管理验证当前/全部空间查看、备份失败不删记录、真实 JSON 下载、原子清除消息与会话，以及通过 UI 导入后恢复记录与空间偏好。
- 原有求职回归现在通过 UI 主动开启应用。该路径复现简历页在无岗位时崩溃，已增加可操作的空状态，相关任务与持久化回归通过。
- agent-browser 查看实际首页与工作空间页面并截图，未报告浏览器错误。
- 独立桌面验收副本 `/tmp/Seeker Round2.app`，标识 `dev.zhapar.seeker.round2`，以本批源码实际编译。真实 UI 创建“桌面阅读空间”、保存助手指令，设置默认名称“桌面日常”并隐藏首页任务，切换空间后退出再启动，名称、指令、当前空间与偏好恢复，求职仍关闭。
- 桌面 UI 导出真实 JSON，检查其中 `platform_projects`、`jh-project`、`seeker-workspace`、`seeker-apps` 与空 profile 均符合预期。测试副本、截图和备份不入库。

默认配置的 `cargo build` 已实际输出 `Compiling app` 并重新嵌入前端。没有推送、打包或发布。Windows 真机与第二轮 AI 质量尚未验证；本批没有新增 AI 调用。

## 第二增量证据

- 新平台“我的作品”提供持久化、重命名、复制、可恢复移除和最近 20 个版本。源内容、样式、来源与空间字段分开保存。
- SQLite 迁移 16 / IndexedDB 版本 11、Runtime 契约、完整便携备份、精简导出排除和 AI 静态硬底已同步。
- 写入比较版本并使用原子事务；并发编辑只有一方成功，事务回滚保留原记录。保存时锁定表单，失败保留输入。知识卡片和六种独立风格提前接入底座，便于无模型验收。
- Widget 自动保存后，聊天消息持有作品引用，重启可以从历史重开。新回归先发现历史入口漏 import，再确认修复；源码与用户文本不进入父应用 HTML。
- 当前交互状态通过受控端口生成快照，单独 nonce iframe 进行本地绘制；不开放同源权限、不改 live 组件。保存 PNG / 长页栅格 PDF 与复制图片使用同一预览。
- 前端 151 项单测及 typecheck 通过；全量 Chromium 42 项通过，作品相关 WebKit 5 项通过。回归覆盖版本、失败保留、复制与撤销、风格独立、备份往返、Widget 历史、长中文与当前输入导出、恶意快照脚本/外链拒绝。
- Rust 242 项单测通过、4 项忽略，parser CLI 1 项通过；fmt、Clippy 通过。新增校验覆盖损坏记录、版本冲突、图片解码、栅格 PDF 与实际文件校验。
- agent-browser 实际操作知识卡片与风格并检查预览。WebKit 的懒加载未就绪和浏览器的零尺寸布局误报先复现再修复；选中作品立即加载，截图等待布局完成，独立画布保持在视口内的透明层。
- 独立桌面副本实际创建“桌面阅读卡”、使用杂志风格、保存中文分行正文，退出再启动后原文和样式恢复。真实 UI 导出 PNG / PDF，`pdfinfo` 确认单页、无脚本，PDF 实际渲染尺寸与 PNG 一致。
- 桌面剪贴板写入后，从系统读回 TIFF，与 748×576 的 PNG 逐像素比较一致。测试图片、PDF 和临时验收副本不入库。

本批手动功能未调用模型；Widget 聊天链用确定性事件验证，真实模型生成质量、Windows 真机未覆盖。
当前 PDF 为图片页；自由 Widget 重新打开时重新运行源码，交互状态只在静态导出时捕获。
个人样式收藏/默认值、思维导图、其余作品类型、比例/署名与离线交互分享继续按原范围实施，不代表整轮已完成。

## 第三增量证据

- 回答、笔记、文件阅读和手动大纲入口接通；平台只接收本次选择的文字。导图支持节点增删改、拖动调整父节点、菜单移动、撤销、折叠、缩放/适配，以及双向、树和大纲视图。
- 内容与独立样式仍分开保存。三种视图使用同一可信 SVG 绘制；新增连线控件。SVG 保留当前可见结构，Markdown 包含折叠子节点，PNG/PDF 复用隔离截图。
- 分支 AI 只发送选中子树；结果先预览，显式应用进入草稿。测试验证无关分支保持逐字段一致，取消、无效结果与预览过期不覆盖用户编辑。生成链用确定性模型响应验证，真实模型质量未覆盖。
- 前端 155 项单测、typecheck 通过；经本地 HTTP 全量 Chromium 47 项通过，导图 WebKit 5 项通过。导图拖动与视图重启保留、文件/回答入口、真实 SVG/Markdown 下载与栅格预览均覆盖。
- Rust 243 项单测通过、4 项忽略，parser CLI 1 项通过；fmt、Clippy 通过。SVG 正向标签和中文实体转义通过，脚本、外链、未知实体、畸形 XML 与多根文档拒绝。
- agent-browser 实际创建中文大纲，未报告浏览器错误。独立桌面副本实际创建“桌面阅读导图”，修改分支、选择手绘风格、保存；退出重启后 5 个节点、修改文字和样式恢复。
- 桌面实际导出 SVG（XML 解析核对全部可见中文标签与 1452×117.5 画布）和 2966×348 PNG，窗口预览包含同一节点与连线。测试文件与副本不入库。

默认配置桌面构建重新嵌入前端；没有打包、推送或发布。个人样式、其余作品类型和分享完善继续实施，整轮尚未完成。

## 第四增量证据

- 六种起点可保存为个人样式，并跨作品复用。设置页管理收藏名称、可撤销移除与新作品默认值；默认值是用户主动保存的副本，不改变已有作品，也不随收藏删除失效。
- 收藏使用 `platform_creations` 的 style 类型；默认值新增 Web/Rust 双端便携偏好白名单。收藏和默认值通过完整备份导出、导入恢复。
- 新建对比表和时间线，支持单元格/事件编辑、行列/事件增删、顺序调整和撤销；复用独立风格、保存历史及隔离图片/PDF 导出。
- 自然语言调整风格只发送样式与要求，正文和默认设置保持原样。自由 Widget AI 修改发送本件源码并在无父窗口动作端口的沙箱预览，显式应用后才进入草稿；版本恢复可还原原稿。
- 前端单测 158 项及 typecheck 通过，完整 Chromium 51 项通过，本批 WebKit 4 项通过。覆盖结构编辑、收藏/默认值/重启、备份往返、写失败保留、AI 字段限制、沙箱预览和历史恢复。
- Rust 243 项单测通过、4 项忽略，parser CLI 1 项通过；fmt、Clippy 通过。新增便携偏好实际包含默认样式且仍排除未知密钥。
- 独立桌面副本实际从导图保存“纸上阅读”样式，在设置中选为默认，再新建“周末阅读方式”对比表。填写纸质书/电子书的中文单元格，保存后检查 750×672 导出预览，内容与手绘风格一致。
- 退出重启后，设置页恢复默认样式“纸上阅读”与同名收藏。默认配置构建实际重新嵌入前端；未打包、推送或发布。

本批 AI 链路使用确定性响应验收，真实模型效果与 Windows 真机未覆盖。分享比例、附加标题/来源/署名、离线交互文件及整轮贯通验收继续实施。

## 第五增量证据

- 导出支持原始长图和五种常用比例，完整缩放内容；附加标题、来源和署名独立勾选，默认不输出，不读取个人信息。PNG、PDF 与复制图片使用同一完成预览。
- Widget 可保存离线交互 HTML，保留源码与独立风格；界面明确重新初始化、不包含当前计算状态，也不应用图片比例。外壳、沙箱与两层 CSP 在 Web / Rust 分别构造，用户源码不进入父页 DOM。
- 新回归先复现“刚编辑即导出”仍得到旧短正文，再验证刷新待绘制预览与等待端口后包含完整 40 行新正文。快速切换比例不会覆盖最新结果。
- 前端 160 项单测及 typecheck 通过，全量 Chromium 55 项通过，分享相关 WebKit 4 项通过。经本地 HTTP 验证真实 PNG 下载尺寸、中文附加文字、离线文件交互与重新初始化、父页访问和外部请求被拒绝，以及英文窄窗口。
- Rust 244 项单测通过、4 项忽略，parser CLI 1 项通过；fmt、Clippy 通过。离线 HTML 校验覆盖超限拒绝、标题/源码转义和 opaque iframe 结构。
- 独立桌面副本通过真实文件选择器导入 145 字节中文 TXT，经本地确定性模型生成 6 节点导图，修改根节点和另一节点，应用“纸上阅读”个人样式并保存 v2；实际导出预览为 2966×554。
- 最后一次比例选择时 Mac 锁屏，工具无法继续操作。16:9 带署名的桌面文件、桌面生成 Widget 后保存离线文件，以及这条资料链的最终重启检查仍待解锁后验收；此前增量已验证桌面 PNG/PDF/剪贴板、导图 SVG 与作品重启恢复。
- 最终默认配置 `cargo build --manifest-path src-tauri/Cargo.toml` 实际输出 `Compiling app` 并通过，前端资源重新嵌入；`git diff --check` 通过。

本地模型夹具只用于验证 UI → Rust → 流式模型响应 → 保存链路，没有评价真实模型质量，也未向云端模型发送测试资料。当前未打包、推送或发布。

## 弹窗调整与恢复验收

- 用户反馈弹窗边框、表单与样式问题后，先在实际桌面窗口截图，确认新作品弹窗缺少现有 `modal-body` / `modal-foot` 容器：正文和表单贴边，导出操作分散，下拉框与输入框高度不一致。
- 作品相关六类弹窗复用统一壳层：固定标题和操作区、可滚动内容区、统一留白、细边框和小圆角；控件统一 40px 高度、聚焦状态与按钮排版。导出按宽度切换左右或上下分区，未勾选的附加文字输入框明确停用。
- 回归先测得输入框距离外边框只有 1px；修复后检查两侧至少 20px，长预览不挤出底部按钮。另先复现固定比例预览溢出 11px，再验证完整适配预览框。
- 前端单测 160 项、typecheck、全量 Chromium 56 项通过；最后预览调整后分享 5 项再次通过，WebKit 分享 5 项通过。WebKit 在弹窗动画中分次测量会读到不同位置，改为完成输入后同帧测量，保留原留白断言。
- 本地浏览器实际查看深色、浅色、保存样式弹窗和 390px 英文窗口；截图待动画完成后采集，确认表单边距、预览与底栏。截图仅为临时验收材料，不入库。
- 最终默认 `cargo build` 实际输出 `Compiling app`，嵌入本次前端并通过。此次只改前端，不增加运行时命令或持久化字段。
- Mac 在加载新版桌面副本前再次锁屏，原来的最终桌面文件与重启验收仍待解锁。本次浏览器效果不能代替新版原生窗口验收，未打包、推送或发布。
