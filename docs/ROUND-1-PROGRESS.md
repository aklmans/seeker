# 第一轮开发记录

完整范围见 [开发计划](ROUND-1-PLAN.md)。此记录区分已经实现的增量与尚未完成的范围，不能作为全部完成的声明。

| 批次 | 状态 | 证据与剩余工作 |
| --- | --- | --- |
| 1 · 入口与会话 | 已实现 | 通用首页、独立会话、新建/重命名、持久化上下文恢复、旧项目历史兼容、通用与求职提示分离；桌面已验证新建、发送、重命名、重启续聊及完成轮次恢复 |
| 2 · 写作与保存 | 已实现 | 四种独立工具、显式保存笔记、收藏/来源/导出、取消自动 AI 整理；桌面已验证未连接时错误、真实请求链路、结果保存、Markdown 落盘、整页布局与重启保留 |
| 3 · 文件阅读 | 待开发 | 四种格式解析、资料保存、受限问答、原文引用、超限与异常 |
| 4 · 资料整理任务 | 待开发 | 通用任务入口、新工作流、输入快照、真实报告与恢复 |
| 5 · 内测验收 | 待开发 | 模型向导、所有用户主链、样例与最终全量验证 |

第一批目前的验证：

- 原基线 139 项 Node 测试通过；新增会话模型测试后 142 项通过，typecheck 通过。
- 浏览器 15 项 E2E 通过，覆盖原有求职与任务路径、新安装默认、真实 Web 请求上下文恢复、新会话隔离、重命名、历史兼容、截断流重试。
- 发现并复现 Web upsert 在请求成功但事务回滚时仍返回成功的问题；修复后该回归用例通过。
- Rust 会话测试覆盖数据库关闭后重开、完整轮次、其他会话/项目与定时历史排除。
- Rust 全量测试 220 项通过、4 项按仓库配置忽略；Clippy 通过。
- 本地 HTTP 页面经 agent-browser 查看与截图，首页及导航可用，未报告浏览器错误。截图为临时验证产物，不入库。

第二批目前的验证：

- Node 144 项通过、typecheck 通过；浏览器全量 24 项 E2E 通过；Rust 224 项通过、4 项忽略；Clippy 通过。
- 保存失败、普通保存不调用 AI、删除事务回滚三项先复现失败再修复。笔记编辑/收藏/来源/搜索/标签/Markdown 下载/删除撤销/重载均覆盖实际浏览器 UI。
- 写作调用仅有 `instruction + untrusted + task`，明确目标语言优先；拒绝未知操作、超限输入、取消、截断与空结果。无工具生成使用专门基线，避免聊天的语言提示覆盖翻译目标。
- 桌面窗口使用本轮编译的临时验收应用（独立标识 `dev.zhapar.seeker.round1`，独立数据目录），未覆盖已安装的应用或用户配置。
- 本机确定性 SSE 服务通过真实桌面协议接收请求：翻译请求仅有 system/user、无工具，包含 Japanese 目标语言；UI 结果成功保存到 SQLite，Markdown 写入 Downloads/Seeker 后重读内容一致。
- 浏览器已查看工具整页布局与截图；测试截图、临时应用和服务日志不入库。
- 桌面重启后真实请求恢复了完整的 user/assistant 轮次，未完成的提问未进入历史；资料库在重启后保留，已截取真实桌面整页布局。
- 重启验收复现本地 Ollama 请求等待云端钥匙串读取的问题，进程采样定位 `load_api_key → SecKeychainFindGenericPassword`。新增测试先失败后通过：Ollama 生成/对话不再读取或转发云端 Key，云端三协议仍要求有效 Key。

第五批仍需整理的现有设置行为：模型页存在过时的托管、语音和职业文案；配置存在性查询仍读取共享 Key 条目，需结合本地协议的无 Key 引导调整。双语撤销文案、首页窄窗口布局与未实现能力的明确降级在最终用户路径验收统一处理。

尚未进行真实模型质量验收与全范围验收。Web 和桌面链路验收使用确定性 SSE 测试响应，不能代表真实模型效果。

工作区已有 `.gitignore` 修改不属于本轮，不纳入提交。当前未推送、未发布。
