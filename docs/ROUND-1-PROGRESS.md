# 第一轮开发记录

完整范围见 [开发计划](ROUND-1-PLAN.md)。此记录区分已经实现的增量与尚未完成的范围，不能作为全部完成的声明。

| 批次 | 状态 | 证据与剩余工作 |
| --- | --- | --- |
| 1 · 入口与会话 | 已实现 | 通用首页、独立会话、新建/重命名、持久化上下文恢复、旧项目历史兼容、通用与求职提示分离；桌面已验证新建、发送、重命名、重启续聊及完成轮次恢复 |
| 2 · 写作与保存 | 已实现 | 四种独立工具、显式保存笔记、收藏/来源/导出、取消自动 AI 整理；桌面已验证未连接时错误、真实请求链路、结果保存、Markdown 落盘、整页布局与重启保留 |
| 3 · 文件阅读 | 已实现 | 四种格式本地解析、原始文件与正文备份、单文件受限问答、逐字引用校验与定位；桌面已验证批量导入、错误引用拒绝、保存和重启保留 |
| 4 · 资料整理任务 | 已实现 | 公共任务中心、应用贡献表单、冻结资料快照、两种真实报告、暂停/恢复/取消、导出与重启找回；完成桌面验证 |
| 5 · 内测验收 | 已实现 | 显式模型连接引导、双语/窄窗口/降级核查、固定材料、报告样例与全量检查；真实模型质量与 Windows 真机仍未覆盖 |

第一批目前的验证：

- 原基线 139 项 Node 测试通过；新增会话模型测试后 142 项通过，typecheck 通过。
- 浏览器 15 项 E2E 通过，覆盖原有求职与任务路径、新安装默认、真实 Web 请求上下文恢复、新会话隔离、重命名、历史兼容、截断流重试。
- 发现并复现 Web upsert 在请求成功但事务回滚时仍返回成功的问题；修复后该回归用例通过。
- Rust 会话测试覆盖数据库关闭后重开、完整轮次、其他会话/项目与定时历史排除。
- Rust 全量测试 220 项通过、4 项按仓库配置忽略；Clippy 通过。
- 本地 HTTP 页面经 agent-browser 查看与截图，首页及导航可用，未报告浏览器错误。截图为临时验证产物，不入库。

第二批目前的验证：

- Node 144 项通过、typecheck 通过；浏览器全量 24 项 E2E 通过；Rust 224 项通过、4 项忽略；Clippy 通过。
- 保存失败、普通保存不调用 AI、删除事务回滚三项先复现失败再修复。笔记编辑/收藏/来源/搜索/标签/Markdown 下载/删除撤销/重载均覆盖实际浏览器 UI。
- 写作调用仅有 `instruction + untrusted + task`，明确目标语言优先；拒绝未知操作、超限输入、取消、截断与空结果。无工具生成使用专门基线，避免聊天的语言提示覆盖翻译目标。
- 桌面窗口使用本轮编译的临时验收应用（独立标识 `dev.zhapar.seeker.round1`，独立数据目录），未覆盖已安装的应用或用户配置。
- 本机确定性 SSE 服务通过真实桌面协议接收请求：翻译请求仅有 system/user、无工具，包含 Japanese 目标语言；UI 结果成功保存到 SQLite，Markdown 写入 Downloads/Seeker 后重读内容一致。
- 浏览器已查看工具整页布局与截图；测试截图、临时应用和服务日志不入库。
- 桌面重启后真实请求恢复了完整的 user/assistant 轮次，未完成的提问未进入历史；资料库在重启后保留，已截取真实桌面整页布局。
- 重启验收复现本地 Ollama 请求等待云端钥匙串读取的问题，进程采样定位 `load_api_key → SecKeychainFindGenericPassword`。新增测试先失败后通过：Ollama 生成/对话不再读取或转发云端 Key，云端三协议仍要求有效 Key。

第五批已处理原模型页的托管、语音假入口与过时文案；本地协议查询跳过共享 Key。撤销提示双语、首页和设置页的完整宽度、Web 未实现能力禁用均已纳入验收；职业训练设置保留在求职应用自己的设置页。

第三批目前的验证：

- Node 146 项、typecheck、浏览器全量 27 项 E2E 通过；Rust 231 项单元测试与 1 项编译程序集成测试通过、4 项忽略；Clippy 通过。
- 解析测试覆盖 Unicode 完整分段、DOCX 段落/表格/实体、文字型 PDF 的真实页码，以及空白、损坏、加密标识和超限拒绝。编译后的程序通过独立 parser 模式处理真实 stdin/stdout，不初始化 GUI。
- 生成终止原因的回归测试先复现 `length` 被错误接受，再修复为只接受完整非空回答；取消仍作为独立控制流。引用校验同时保留正确引文和未知 ID、伪造引文、缺失引文的正反用例。
- Web 经本地 HTTP 验证资料预览、Markdown 下载、引用定位、显式保存、错误重试、取消、信息不足提示、原始字节随完整备份保留、脱敏导出排除、删除撤销和重载保留。
- 真实桌面文件选择器一次导入 TXT、MD、DOCX 和损坏 PDF：三份成功，损坏文件独立报错；DOCX 表格文字可见。SQLite 中原始字节与用户选择的测试文件逐一比较相同，SHA-256 与字数一致。
- 桌面固定模型响应走真实供应商协议与 Rust 引用校验：请求仅有 system/user，所选正文被框定，没有其他笔记、会话或工具；正确引文跳回原文并保存笔记，故意伪造的引文被拒绝且问题保留。
- 重启桌面验收副本后，首页可找回三个文件与阅读笔记，打开 DOCX 仍有原文和表格文字；截图与临时文件不入库。

五条用户路径已完成功能验收。Web 和桌面链路使用确定性 SSE 测试响应，真实模型质量未覆盖，不能把接口连通和文件完整性当作模型效果。

第四批目前的验证：

- Node 147 项、typecheck、浏览器全量 30 项通过；Rust 238 项单元测试和 1 项编译程序集成测试通过、4 项忽略；Clippy 通过。
- 两份真实报告覆盖正文和 DOCX 内部 XML；测试覆盖未选择资料/非正文隐私字段排除、原资料删除后恢复仍使用快照、伪造引用和修改计划拒绝、暂停保留已完成步骤、取消后不继续，以及文件写入失败恢复不重复模型步骤。
- 浏览器验证公共任务入口不依赖启用求职应用，创建失败保留输入，选择后才创建快照、开始后才执行；任务状态操作、预览、显式保存、导出副本和首页最近任务均有正向断言。浏览器执行使用 Runtime fixture；真实文件由 Rust 和桌面链路验证。
- 桌面从真实 UI 选择两个文件和一条阅读笔记，创建后检查三份快照，再运行 8 步得到两个已验证报告。实际协议共 4 次无工具调用，仅携带选中材料，无其他会话或笔记。
- 实际导出的 MD/DOCX 与已验证产物字节相同，SHA-256 一致，DOCX 正文含真实源文；重启后可从首页找回并预览报告。
- 桌面预览复现转义标点显示反斜杠的问题，先添加失败测试再修复安全 Markdown 渲染；合法格式、转义字符、代码原文、链接与 HTML 注入边界一并验证。更新后的真实窗口中引用和文件名正常。
- 所有测试服务响应均为固定 fixture，仍不代表真实模型的整理质量。

第五批验证：

- Node 147 项、typecheck、浏览器 33 项通过；Rust 239 项单元测试、1 项编译程序集成测试通过，4 项按仓库配置忽略；fmt、Clippy 和实际嵌入前端的构建通过。
- 模型设置先读取旧配置，切换协议不自动写入；保存/Key/连接各阶段失败有明确提示，Key 不进入普通配置，连接测试仅发送固定短句。连接失败、鉴权失败和截断回应均有拒绝断言。
- 本地配置查询的回归测试先复现云端钥匙串调用，再修复。云端三协议仍保留状态检查，Ollama 配置读取与生成均不读云端 Key。
- 真实桌面保留先前的 Ollama 地址/模型/高级配置，检查不存在的本机端口得到失败状态，恢复测试地址后连通；请求记录仅有固定测试文本。英文窄窗口首页已截图，直接发送会新建实际持久化对话。
- 首页输入转交既有聊天链，无第二套历史状态；重载续聊与新会话隔离 E2E 继续通过。英文撤销按钮和完成提示通过测试，Web 笔记 AI 整理明确禁用。
- 固定材料生成脚本产生 TXT/MD、两页文字 PDF、含表格 DOCX、无文字/损坏 PDF 与超限正文；全部经过实际编译程序的 parser 模式，四份成功、三份拒绝，页码与表格内容正确。
- [快速开始](QUICKSTART.md)、[验收步骤和未覆盖项](ROUND-1-ACCEPTANCE.md)、[固定报告样例](examples/material-report-fixture.md) 已更新。本机 Ollama 模型列表为空，未下载模型、未使用其他账户的凭据，真实模型质量待可用服务补验。

工作区已有 `.gitignore` 修改不属于本轮，不纳入提交。当前未推送、未发布。
